# 認証導入（フェーズ3） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `streamlit-authenticator`によるログイン・ログアウト・自己サービス新規登録・パスワード変更を導入し、フェーズ1で固定していた`DEFAULT_USER_ID = 1`を実際の認証済みユーザーIDに置き換える（`docs/superpowers/specs/2026-08-10-database-multiuser-auth-design.md`のフェーズ3）。

**Architecture:** `auth.py`（新規）がDBの`User`テーブルとstreamlit-authenticatorの`credentials`辞書を仲介する。`app.py`冒頭にログインゲートを追加し、認証成功時のみ`st.session_state["user_id"]`を設定して以降のタブを描画する。`app_tabs/shared.py`の`DEFAULT_USER_ID`定数は`get_current_user_id()`関数（`st.session_state["user_id"]`を返すだけ）に置き換え、既存の6箇所の呼び出しを機械的に更新する。

**Tech Stack:** `streamlit-authenticator==0.4.2`（実際にインストールし、ソースコード（`AuthenticationModel`/`AuthenticationController`/`Authenticate`）を直接確認して以下の挙動を検証済み）。

- パスワードハッシュは`bcrypt`（`Hasher.hash`/`Hasher.check_pw`はいずれも`bcrypt.hashpw`/`bcrypt.checkpw`を直接使用）。フェーズ1の`scripts/migrate_to_db.py`が既に`bcrypt.hashpw(...).decode("utf-8")`でハッシュ化して`User.hashed_password`へ保存しているため、**既存ユーザーのパスワードハッシュはそのまま使える**（再ハッシュ不要）
- `credentials`辞書の形は`{"usernames": {username: {"email":..., "password": <bcryptハッシュ>, "first_name":..., "last_name":..., ...}}}`。表示名は`first_name`/`last_name`から組み立てられ、両方無ければ`name`キーにフォールバックする（`AuthenticationModel._get_user_name`: `f"{first_name} {last_name}".strip() or name`）
- `authenticator.login()`は`location='main'`（デフォルト）ではフォームを描画するのみで戻り値は使わず、代わりに`st.session_state["authentication_status"]`/`["username"]`/`["name"]`を見る（公式`__init__.py`のドキュメント例と同じパターン）
- `authenticator.register_user(captcha=False)`はフォーム送信時に`(email, username, name)`を**同期的に返す**（`None, None, None`ではなく実値が返ればその場で成功と判定できる）。登録失敗時は`RegisterError`を送出する
- `authenticator.reset_password(username, location=...)`はログイン中のみ使え、成功時`True`を返す。失敗時は`CredentialsError`/`ResetError`を送出する
- 新規登録・パスワード変更で生成されたbcryptハッシュ済みパスワードは、`authenticator.authentication_controller.authentication_model.credentials["usernames"][username]["password"]`から取り出せる（ライブラリの公開属性を辿るだけで、プライベートAPIではない）

## Global Constraints

- Python >=3.14、パッケージ管理は`uv`（`ai-stock-investing-tutorial/app/pyproject.toml`）。`streamlit-authenticator`は既に`uv add streamlit-authenticator`で追加済み（`pyproject.toml`に`streamlit-authenticator>=0.4.2`、`uv.lock`更新済み、未コミット）
- テストは`uv run pytest -v`（`app/`ディレクトリで実行）
- DBを使うテストは`tmp_path`上のファイルDB（`sqlite:///{tmp_path}/test.db`）を使う
- DB操作を行う関数は`session_factory`引数（デフォルトは本番用`db.engine.SessionLocal`）を受け取り、テストでは注入する（フェーズ1・2と同じDIパターン）
- streamlit-authenticatorのウィジェット自体（`login`/`register_user`/`logout`/`reset_password`のUI描画）はユニットテスト対象外（既存の`app_tabs`と同じ慣習）。`auth.py`のDB連携ロジック（`build_credentials`/`get_user_id`/`persist_new_user`/`persist_password_update`）のみユニットテストする
- リポジトリはmasterへの直接コミット運用（フィーチャーブランチ・worktreeは使わない）
- 実運用中の`data/app.db`は既にフェーズ1で作成済み（`users`テーブルに`first_name`/`last_name`列が無い状態）。Alembic等は使わない方針のため、`init_db()`に軽量なカラム追加処理を組み込み、既存DBを起動時に自動的に追従させる

---

### Task 1: Userモデルへの`first_name`/`last_name`追加・既存DBへの自動列追加

**Files:**
- Modify: `ai-stock-investing-tutorial/app/db/models.py`
- Modify: `ai-stock-investing-tutorial/app/db/engine.py`
- Modify: `ai-stock-investing-tutorial/app/tests/test_db_engine.py`

**Interfaces:**
- Produces:
  - `db.models.User`に`first_name: Mapped[str | None]`・`last_name: Mapped[str | None]`を追加
  - `db.engine.init_db(engine)`は`create_all()`後、既存の`users`テーブルに`first_name`/`last_name`列が無ければ`ALTER TABLE`で追加する（新規作成時はcreate_allが最初から両方の列を含むテーブルを作るため、既存DBのみが対象）

- [ ] **Step 1: 失敗するテストを書く**

`ai-stock-investing-tutorial/app/tests/test_db_engine.py`の末尾に追加:

```python
def test_init_db_adds_missing_user_name_columns_to_existing_table(tmp_path):
    from sqlalchemy import text

    engine = create_db_engine(f"sqlite:///{tmp_path / 'test.db'}")
    # フェーズ3以前（first_name/last_name追加前）のusersテーブルを模して作成する
    with engine.connect() as connection:
        connection.execute(
            text(
                "CREATE TABLE users ("
                "id INTEGER PRIMARY KEY, username TEXT UNIQUE NOT NULL, "
                "email TEXT UNIQUE, hashed_password TEXT NOT NULL, created_at DATETIME)"
            )
        )
        connection.commit()

    init_db(engine)

    with engine.connect() as connection:
        columns = {
            row[1] for row in connection.execute(text("PRAGMA table_info(users)")).fetchall()
        }
    assert "first_name" in columns
    assert "last_name" in columns


def test_init_db_is_idempotent_on_second_call(tmp_path):
    engine = create_db_engine(f"sqlite:///{tmp_path / 'test.db'}")
    init_db(engine)
    init_db(engine)
    table_names = set(inspect(engine).get_table_names())
    assert "users" in table_names


def test_user_stores_first_name_and_last_name(tmp_path):
    engine = create_db_engine(f"sqlite:///{tmp_path / 'test.db'}")
    init_db(engine)
    session_factory = sessionmaker(bind=engine)

    with session_factory() as session:
        session.add(
            User(
                username="taro",
                hashed_password="hashed",
                first_name="太郎",
                last_name="山田",
            )
        )
        session.commit()

    with session_factory() as session:
        stored = session.query(User).filter_by(username="taro").one()
        assert stored.first_name == "太郎"
        assert stored.last_name == "山田"
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `uv run pytest tests/test_db_engine.py -v -k "user_name_columns or idempotent or first_name"`
Expected: FAIL（`User.__init__`が`first_name`を受け付けない、または`ALTER TABLE`が実行されず列が追加されない）

- [ ] **Step 3: `db/models.py`に列を追加する**

`ai-stock-investing-tutorial/app/db/models.py`、変更前:

```python
class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True)
    username: Mapped[str] = mapped_column(unique=True, nullable=False)
    email: Mapped[str | None] = mapped_column(unique=True, nullable=True)
    hashed_password: Mapped[str] = mapped_column(nullable=False)
    created_at: Mapped[datetime.datetime] = mapped_column(default=_utcnow)
```

変更後:

```python
class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True)
    username: Mapped[str] = mapped_column(unique=True, nullable=False)
    email: Mapped[str | None] = mapped_column(unique=True, nullable=True)
    hashed_password: Mapped[str] = mapped_column(nullable=False)
    first_name: Mapped[str | None] = mapped_column(nullable=True)
    last_name: Mapped[str | None] = mapped_column(nullable=True)
    created_at: Mapped[datetime.datetime] = mapped_column(default=_utcnow)
```

- [ ] **Step 4: `db/engine.py`に既存DBへの列追加処理を実装する**

`ai-stock-investing-tutorial/app/db/engine.py`、変更前:

```python
from sqlalchemy import create_engine
from sqlalchemy.engine import Engine
from sqlalchemy.orm import sessionmaker

from db.models import Base
```

変更後:

```python
from sqlalchemy import create_engine, text
from sqlalchemy.engine import Engine
from sqlalchemy.orm import sessionmaker

from db.models import Base
```

`init_db`関数、変更前:

```python
def init_db(engine: Engine) -> None:
    """未作成のテーブルのみ作成する（既存テーブルには影響しない）。"""
    Base.metadata.create_all(engine)
```

変更後:

```python
def init_db(engine: Engine) -> None:
    """未作成のテーブルのみ作成する（既存テーブルには影響しない）。加えて、既存の
    usersテーブルにfirst_name/last_name列が無ければALTER TABLEで追加する
    （フェーズ3で追加した列。Alembic等の本格的なマイグレーションツールは使わない
    方針のため、この程度の単純な追加列はここで直接吸収する。新規作成時は
    create_all()が最初から両方の列を含むテーブルを作るため対象外）。"""
    Base.metadata.create_all(engine)
    _ensure_user_name_columns(engine)


def _ensure_user_name_columns(engine: Engine) -> None:
    with engine.connect() as connection:
        existing_columns = {
            row[1] for row in connection.execute(text("PRAGMA table_info(users)")).fetchall()
        }
        for column in ("first_name", "last_name"):
            if column not in existing_columns:
                connection.execute(text(f"ALTER TABLE users ADD COLUMN {column} TEXT"))
        connection.commit()
```

- [ ] **Step 5: テストを実行して通ることを確認する**

Run: `uv run pytest tests/test_db_engine.py -v`
Expected: PASS（全件）

- [ ] **Step 6: 全体テストスイートを実行して回帰が無いことを確認する**

Run: `uv run pytest -v`
Expected: PASS（全件）

- [ ] **Step 7: Commit**

```bash
git add pyproject.toml uv.lock db/models.py db/engine.py tests/test_db_engine.py
git commit -m "feat: streamlit-authenticator導入・Userにfirst_name/last_nameを追加（既存DBへの自動列追加込み）"
```

（`pyproject.toml`/`uv.lock`は`uv add streamlit-authenticator`実行時に既に更新済みのため、このコミットでまとめて取り込む）

---

### Task 2: `auth.py`（credentials辞書構築・ユーザー永続化）

**Files:**
- Create: `ai-stock-investing-tutorial/app/auth.py`
- Create: `ai-stock-investing-tutorial/app/tests/test_auth.py`

**Interfaces:**
- Consumes: `db.engine.SessionLocal`、`db.models.User`（Task 1）
- Produces:
  - `auth.build_credentials(session_factory=SessionLocal) -> dict` — `{"usernames": {username: {...}}}`
  - `auth.get_user_id(username: str, session_factory=SessionLocal) -> int | None`
  - `auth.persist_new_user(username, email, hashed_password, first_name=None, last_name=None, session_factory=SessionLocal) -> User`
  - `auth.persist_password_update(username, hashed_password, session_factory=SessionLocal) -> None`

- [ ] **Step 1: 失敗するテストを書く**

`ai-stock-investing-tutorial/app/tests/test_auth.py`:

```python
import pytest
from sqlalchemy.orm import sessionmaker

from auth import build_credentials, get_user_id, persist_new_user, persist_password_update
from db.engine import create_db_engine, init_db
from db.models import User


@pytest.fixture
def session_factory(tmp_path):
    engine = create_db_engine(f"sqlite:///{tmp_path / 'test.db'}")
    init_db(engine)
    return sessionmaker(bind=engine)


def test_build_credentials_uses_first_and_last_name_when_present(session_factory):
    with session_factory() as session:
        session.add(
            User(
                username="taro",
                email="taro@example.com",
                hashed_password="hashed",
                first_name="太郎",
                last_name="山田",
            )
        )
        session.commit()

    credentials = build_credentials(session_factory=session_factory)
    entry = credentials["usernames"]["taro"]
    assert entry["email"] == "taro@example.com"
    assert entry["password"] == "hashed"
    assert entry["first_name"] == "太郎"
    assert entry["last_name"] == "山田"
    assert "name" not in entry


def test_build_credentials_falls_back_to_username_when_no_name(session_factory):
    with session_factory() as session:
        session.add(User(username="admin", email=None, hashed_password="hashed"))
        session.commit()

    credentials = build_credentials(session_factory=session_factory)
    entry = credentials["usernames"]["admin"]
    assert entry["name"] == "admin"
    assert "first_name" not in entry
    assert "last_name" not in entry


def test_build_credentials_returns_empty_usernames_when_no_users(session_factory):
    assert build_credentials(session_factory=session_factory) == {"usernames": {}}


def test_get_user_id_returns_id_for_existing_username(session_factory):
    with session_factory() as session:
        user = User(username="taro", hashed_password="hashed")
        session.add(user)
        session.commit()
        session.refresh(user)
        expected_id = user.id

    assert get_user_id("taro", session_factory=session_factory) == expected_id


def test_get_user_id_returns_none_for_unknown_username(session_factory):
    assert get_user_id("nobody", session_factory=session_factory) is None


def test_persist_new_user_inserts_row(session_factory):
    user = persist_new_user(
        "taro", "taro@example.com", "hashed-pw", "太郎", "山田", session_factory=session_factory
    )
    assert user.id is not None

    with session_factory() as session:
        stored = session.query(User).filter_by(username="taro").one()
        assert stored.email == "taro@example.com"
        assert stored.hashed_password == "hashed-pw"
        assert stored.first_name == "太郎"
        assert stored.last_name == "山田"


def test_persist_password_update_updates_hash(session_factory):
    persist_new_user("taro", None, "old-hash", session_factory=session_factory)
    persist_password_update("taro", "new-hash", session_factory=session_factory)

    with session_factory() as session:
        stored = session.query(User).filter_by(username="taro").one()
        assert stored.hashed_password == "new-hash"
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `uv run pytest tests/test_auth.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'auth'`）

- [ ] **Step 3: `auth.py`を実装する**

`ai-stock-investing-tutorial/app/auth.py`:

```python
"""streamlit-authenticatorとDB（Userテーブル）を仲介するモジュール。
credentials辞書の構築、新規登録・パスワード変更結果の永続化を担う。
"""

from db.engine import SessionLocal
from db.models import User


def build_credentials(session_factory=SessionLocal) -> dict:
    """DBのUserテーブル全件からstreamlit-authenticatorが要求するcredentials辞書を
    組み立てる。first_name/last_nameが未設定のユーザー（フェーズ1の移行スクリプトで
    作成した管理者アカウント等）はusernameを表示名としてフォールバックする
    （AuthenticationModel._get_user_nameがfirst_name/last_name不在時に"name"キーを
    参照する挙動に合わせる）。"""
    with session_factory() as session:
        users = session.query(User).all()
        usernames = {}
        for user in users:
            entry = {"email": user.email, "password": user.hashed_password}
            if user.first_name or user.last_name:
                entry["first_name"] = user.first_name or ""
                entry["last_name"] = user.last_name or ""
            else:
                entry["name"] = user.username
            usernames[user.username] = entry
        return {"usernames": usernames}


def get_user_id(username: str, session_factory=SessionLocal) -> int | None:
    """ユーザー名からユーザーIDを引き当てる。存在しなければNoneを返す。"""
    with session_factory() as session:
        user = session.query(User).filter_by(username=username).first()
        return user.id if user else None


def persist_new_user(
    username: str,
    email: str | None,
    hashed_password: str,
    first_name: str | None = None,
    last_name: str | None = None,
    session_factory=SessionLocal,
) -> User:
    """streamlit-authenticatorのregister_user()ウィジェットが成功した後、その結果
    （既にbcryptハッシュ済みのパスワードを含む）をUserテーブルへ永続化する。"""
    with session_factory() as session:
        user = User(
            username=username,
            email=email,
            hashed_password=hashed_password,
            first_name=first_name,
            last_name=last_name,
        )
        session.add(user)
        session.commit()
        session.refresh(user)
        return user


def persist_password_update(
    username: str, hashed_password: str, session_factory=SessionLocal
) -> None:
    """streamlit-authenticatorのreset_password()ウィジェットが成功した後、その結果
    （既にbcryptハッシュ済みのパスワードを含む）をUserテーブルへ反映する。"""
    with session_factory() as session:
        user = session.query(User).filter_by(username=username).first()
        user.hashed_password = hashed_password
        session.commit()
```

- [ ] **Step 4: テストを実行して通ることを確認する**

Run: `uv run pytest tests/test_auth.py -v`
Expected: PASS（8件）

- [ ] **Step 5: Commit**

```bash
git add auth.py tests/test_auth.py
git commit -m "feat: streamlit-authenticator用のcredentials構築・ユーザー永続化モジュールを追加"
```

---

### Task 3: `app_tabs/shared.py`の`DEFAULT_USER_ID`を`get_current_user_id()`に置き換え

**Files:**
- Modify: `ai-stock-investing-tutorial/app/app_tabs/shared.py`
- Modify: `ai-stock-investing-tutorial/app/app_tabs/portfolio_tab.py`
- Modify: `ai-stock-investing-tutorial/app/app_tabs/qa_tab.py`
- Modify: `ai-stock-investing-tutorial/app/app_tabs/ranking_tab.py`
- Modify: `ai-stock-investing-tutorial/app/app_tabs/strategy_builder_tab.py`
- Modify: `ai-stock-investing-tutorial/app/app_tabs/sector/tab.py`

**Interfaces:**
- Produces: `app_tabs.shared.get_current_user_id() -> int`（`st.session_state["user_id"]`を返す。この値はTask 4でapp.pyがログイン成功時に設定する）

このタスクは`app_tabs`配下のみの機械的な置き換えで、既存関数の呼び出し引数を変えるだけ（`app_tabs`配下は元々ユニットテスト対象外）。各編集後に全体テストスイートで回帰が無いことを確認する。

- [ ] **Step 1: `app_tabs/shared.py`の定数を関数に置き換える**

`ai-stock-investing-tutorial/app/app_tabs/shared.py`、変更前:

```python
# フェーズ3（認証導入）で認証済みユーザーのIDに置き換えるまでの暫定値。
# scripts/migrate_to_db.pyが作成する最初のユーザーのIDと一致する。
DEFAULT_USER_ID = 1
```

変更後:

```python
def get_current_user_id() -> int:
    """ログイン中のユーザーのIDを返す。app.pyがログイン成功時に
    st.session_state["user_id"]を設定するため、このタブ描画コードに到達する時点では
    必ず設定済み。"""
    return st.session_state["user_id"]
```

- [ ] **Step 2: `app_tabs/portfolio_tab.py`を更新する**

`ai-stock-investing-tutorial/app/app_tabs/portfolio_tab.py:20-28`、変更前:

```python
from app_tabs.shared import (
    CACHE_DIR,
    DEFAULT_USER_ID,
    cached_analyze_fundamentals,
    cached_fetch_japanese_name,
    cached_fetch_news,
    cached_fetch_price_history,
    handle_table_selection,
)
```

変更後:

```python
from app_tabs.shared import (
    CACHE_DIR,
    cached_analyze_fundamentals,
    cached_fetch_japanese_name,
    cached_fetch_news,
    cached_fetch_price_history,
    get_current_user_id,
    handle_table_selection,
)
```

`portfolio_tab.py:39`、変更前: `st.session_state["holdings_rows"] = load_holdings(DEFAULT_USER_ID)`
変更後: `st.session_state["holdings_rows"] = load_holdings(get_current_user_id())`

`portfolio_tab.py`の2箇所の`save_holdings(DEFAULT_USER_ID, holdings)`（120行目・127行目）を、いずれも`save_holdings(get_current_user_id(), holdings)`に置き換える。

- [ ] **Step 3: `app_tabs/qa_tab.py`を更新する**

`ai-stock-investing-tutorial/app/app_tabs/qa_tab.py:23-28`、変更前:

```python
from app_tabs.shared import (
    DEFAULT_USER_ID,
    cached_analyze_fundamentals,
    cached_fetch_news,
    cached_fetch_price_history,
)
```

変更後:

```python
from app_tabs.shared import (
    cached_analyze_fundamentals,
    cached_fetch_news,
    cached_fetch_price_history,
    get_current_user_id,
)
```

`qa_tab.py:43`、変更前: `holdings = load_holdings(DEFAULT_USER_ID)`
変更後: `holdings = load_holdings(get_current_user_id())`

- [ ] **Step 4: `app_tabs/ranking_tab.py`を更新する**

`ai-stock-investing-tutorial/app/app_tabs/ranking_tab.py:21-27`、変更前:

```python
from app_tabs.shared import (
    CACHE_DIR,
    DEFAULT_USER_ID,
    cached_fetch_japanese_name,
    cached_fetch_price_history,
    handle_table_selection,
)
```

変更後:

```python
from app_tabs.shared import (
    CACHE_DIR,
    cached_fetch_japanese_name,
    cached_fetch_price_history,
    get_current_user_id,
    handle_table_selection,
)
```

`ranking_tab.py:59`、変更前: `holdings = load_holdings(DEFAULT_USER_ID)`
変更後: `holdings = load_holdings(get_current_user_id())`

`ranking_tab.py:127`、変更前:

```python
        candidate_names = build_candidate_names(
            load_holdings(DEFAULT_USER_ID), resolve_name=cached_fetch_japanese_name
        )
```

変更後:

```python
        candidate_names = build_candidate_names(
            load_holdings(get_current_user_id()), resolve_name=cached_fetch_japanese_name
        )
```

- [ ] **Step 5: `app_tabs/strategy_builder_tab.py`を更新する**

`ai-stock-investing-tutorial/app/app_tabs/strategy_builder_tab.py:31-37`、変更前:

```python
from app_tabs.shared import (
    DEFAULT_USER_ID,
    handle_table_selection,
    render_mermaid,
    run_or_load_sector_rotation,
)
```

変更後:

```python
from app_tabs.shared import (
    get_current_user_id,
    handle_table_selection,
    render_mermaid,
    run_or_load_sector_rotation,
)
```

`strategy_builder_tab.py:139`、変更前: `saved_strategies = load_strategies(DEFAULT_USER_ID)`
変更後: `saved_strategies = load_strategies(get_current_user_id())`

`strategy_builder_tab.py:195`、変更前: `save_strategy(DEFAULT_USER_ID, pending)`
変更後: `save_strategy(get_current_user_id(), pending)`

- [ ] **Step 6: `app_tabs/sector/tab.py`を更新する**

`ai-stock-investing-tutorial/app/app_tabs/sector/tab.py:22`、変更前:

```python
from app_tabs.shared import DEFAULT_USER_ID, run_or_load_sector_rotation
```

変更後:

```python
from app_tabs.shared import get_current_user_id, run_or_load_sector_rotation
```

`sector/tab.py:36`、変更前: `display_settings = load_sector_display_settings(DEFAULT_USER_ID)`
変更後: `display_settings = load_sector_display_settings(get_current_user_id())`

`sector/tab.py:106`、変更前: `save_sector_display_settings(DEFAULT_USER_ID, new_display_settings)`
変更後: `save_sector_display_settings(get_current_user_id(), new_display_settings)`

- [ ] **Step 7: 全体テストスイートを実行して回帰が無いことを確認する**

Run: `uv run pytest -v`
Expected: PASS（全件）

- [ ] **Step 8: Commit**

```bash
git add app_tabs/shared.py app_tabs/portfolio_tab.py app_tabs/qa_tab.py app_tabs/ranking_tab.py app_tabs/strategy_builder_tab.py app_tabs/sector/tab.py
git commit -m "refactor: DEFAULT_USER_ID固定値をget_current_user_id()（認証済みユーザー）に置き換え"
```

---

### Task 4: `app.py`への認証統合（ログインゲート・新規登録・ログアウト・パスワード変更）

**Files:**
- Modify: `ai-stock-investing-tutorial/app/app.py`

**Interfaces:**
- Consumes: `auth.build_credentials`/`get_user_id`/`persist_new_user`/`persist_password_update`（Task 2）、`app_tabs.shared.get_current_user_id`（Task 3が定義、app.py内では直接は呼ばない）
- Produces: 認証成功時のみ`st.session_state["user_id"]`が設定され、以降のタブ描画に到達する

このタスクはUIウィジェットの結線でありユニットテスト対象外（既存の`app.py`の慣習どおり）。Step 4で手動起動して確認する。

- [ ] **Step 1: importを追加する**

`ai-stock-investing-tutorial/app/app.py:9-16`、変更前:

```python
import logging

import streamlit as st

from common.disclaimer import DISCLAIMER_NOTICE
from common.logging_config import setup_logging
from data_api.llm_client import check_claude_cli_available
from db.engine import engine, init_db
```

変更後:

```python
import logging

import streamlit as st
import streamlit_authenticator as stauth

from auth import build_credentials, get_user_id, persist_new_user, persist_password_update
from common.disclaimer import DISCLAIMER_NOTICE
from common.logging_config import setup_logging
from data_api.llm_client import check_claude_cli_available
from db.engine import engine, init_db
```

- [ ] **Step 2: Claude CLIチェックの直後にログインゲートを追加する**

`ai-stock-investing-tutorial/app/app.py`、変更前:

```python
# Claude CLIが利用できない環境ではLLM機能が動作しないため、起動時点でチェックしてアプリを止める
try:
    check_claude_cli_available()
except Exception as exc:
    st.error(str(exc))
    st.stop()

st.sidebar.markdown(DISCLAIMER_NOTICE)
```

変更後:

```python
# Claude CLIが利用できない環境ではLLM機能が動作しないため、起動時点でチェックしてアプリを止める
try:
    check_claude_cli_available()
except Exception as exc:
    st.error(str(exc))
    st.stop()

# --- 認証ゲート ---
# Cookie署名キーは.streamlit/secrets.tomlに保存する（README.mdのセットアップ手順を参照）。
# リポジトリには含めない機密情報のため、未設定ならここでアプリを止める。
try:
    auth_cookie_key = st.secrets["auth_cookie_key"]
except Exception:
    st.error(
        "認証用のCookie署名キーが設定されていません。"
        "`.streamlit/secrets.toml` に `auth_cookie_key = \"...\"` を設定してください"
        "（README.mdのセットアップ手順を参照）。"
    )
    st.stop()

authenticator = stauth.Authenticate(
    build_credentials(),
    cookie_name="stock_advisor_auth",
    cookie_key=auth_cookie_key,
    cookie_expiry_days=30,
)

try:
    authenticator.login()
except stauth.LoginError as exc:
    st.error(str(exc))

auth_status = st.session_state.get("authentication_status")

if auth_status is False:
    st.error("ユーザー名またはパスワードが正しくありません。")

if auth_status is not True:
    st.divider()
    st.subheader("新規登録")
    try:
        new_email, new_username, new_name = authenticator.register_user(captcha=False)
        if new_email:
            entry = authenticator.authentication_controller.authentication_model.credentials[
                "usernames"
            ][new_username]
            persist_new_user(
                new_username,
                entry.get("email"),
                entry["password"],
                entry.get("first_name"),
                entry.get("last_name"),
            )
            st.success("登録が完了しました。上のフォームからログインしてください。")
    except stauth.RegisterError as exc:
        st.error(str(exc))
    st.stop()

if "user_id" not in st.session_state:
    st.session_state["user_id"] = get_user_id(st.session_state["username"])

authenticator.logout(location="sidebar")
st.sidebar.caption(
    f"ログイン中: {st.session_state.get('name') or st.session_state['username']}"
)
st.sidebar.divider()
st.sidebar.subheader("パスワード変更")
try:
    if authenticator.reset_password(st.session_state["username"], location="sidebar"):
        entry = authenticator.authentication_controller.authentication_model.credentials[
            "usernames"
        ][st.session_state["username"]]
        persist_password_update(st.session_state["username"], entry["password"])
        st.sidebar.success("パスワードを変更しました。")
except (stauth.CredentialsError, stauth.ResetError) as exc:
    st.sidebar.error(str(exc))

st.sidebar.markdown(DISCLAIMER_NOTICE)
```

- [ ] **Step 3: 構文確認**

Run: `uv run python -c "import ast; ast.parse(open('app.py', encoding='utf-8').read())"`
Expected: エラーなく終了する（構文エラーが無いことの簡易確認。Streamlit実アプリとしての動作確認はTask 6で行う）

- [ ] **Step 4: 全体テストスイートを実行して回帰が無いことを確認する**

Run: `uv run pytest -v`
Expected: PASS（全件。`app.py`はユニットテスト対象外のためこのタスクで新規に壊れるテストは無いはず）

- [ ] **Step 5: Commit**

```bash
git add app.py
git commit -m "feat: app.pyにstreamlit-authenticatorによるログイン・新規登録・ログアウト・パスワード変更を統合"
```

---

### Task 5: `.streamlit/secrets.toml`セットアップ・`.gitignore`追加・README追記

**Files:**
- Create: `ai-stock-investing-tutorial/app/.streamlit/secrets.toml`（ローカル専用、コミットしない）
- Modify: `ai-stock-investing-tutorial/app/.gitignore`
- Modify: `ai-stock-investing-tutorial/app/README.md`

**Interfaces:** なし（設定ファイル・ドキュメントのみ）

- [ ] **Step 1: `.gitignore`に追加する**

`ai-stock-investing-tutorial/app/.gitignore`、変更前:

```
# Env
.env

# Runtime data
data/
logs/
```

変更後:

```
# Env
.env

# Streamlit secrets（認証用Cookie署名キーなど、コミットしない）
.streamlit/secrets.toml

# Runtime data
data/
logs/
```

- [ ] **Step 2: ローカル用の`.streamlit/secrets.toml`を作成する**

`ai-stock-investing-tutorial/app`ディレクトリで:

```bash
mkdir -p .streamlit
uv run python -c "import secrets; print(f'auth_cookie_key = \"{secrets.token_hex(32)}\"')" > .streamlit/secrets.toml
cat .streamlit/secrets.toml
```

Expected: `auth_cookie_key = "<64文字の16進数文字列>"`という1行のファイルが作成される

- [ ] **Step 3: README.mdにセットアップ手順を追記する**

`ai-stock-investing-tutorial/app/README.md`の「## セットアップ」節、変更前:

```markdown
## セットアップ

```bash
cd app
uv sync
```

## 起動
```

変更後:

```markdown
## セットアップ

```bash
cd app
uv sync
```

認証用のCookie署名キーをローカル専用の`.streamlit/secrets.toml`に設定してください（`.gitignore`対象、リポジトリには含まれません）。

```bash
mkdir -p .streamlit
python -c "import secrets; print(f'auth_cookie_key = \"{secrets.token_hex(32)}\"')" > .streamlit/secrets.toml
```

初回起動後、画面の「新規登録」フォームからアカウントを作成してください。

## 起動
```

- [ ] **Step 4: Commit**

```bash
git add .gitignore README.md
git commit -m "docs: 認証用secrets.tomlのセットアップ手順を追加"
```

（`.streamlit/secrets.toml`自体は`.gitignore`対象のためコミットしない）

---

### Task 6: 全体テスト・実データでの動作確認・手動ブラウザ確認

このタスクは新規コードを書かず、フェーズ3全体が実際のアプリで動くことを確認する。

**Files:** なし（確認のみ）

- [ ] **Step 1: 全体テストスイートを実行する**

Run: `uv run pytest -v`（`ai-stock-investing-tutorial/app`ディレクトリで）
Expected: 全件PASS

- [ ] **Step 2: 実データ（`data/app.db`）が存在する場合はバックアップする**

```bash
cp data/app.db data/app.db.backup-before-auth
```

- [ ] **Step 3: アプリを起動する**

```bash
uv run python -m streamlit run app.py
```

- [ ] **Step 4: 既存DBへの列追加を確認する**

ターミナルのログにエラーが出ていないこと、および以下で`users`テーブルに`first_name`/`last_name`列が追加されていることを確認する:

```bash
uv run python -c "
import sqlite3
conn = sqlite3.connect('data/app.db')
columns = [row[1] for row in conn.execute('PRAGMA table_info(users)')]
print(columns)
assert 'first_name' in columns and 'last_name' in columns
print('OK: 既存DBに first_name/last_name が追加されました')
"
```

- [ ] **Step 5: ブラウザで動作確認する**

`http://localhost:8501`を開き、以下を確認する:

- 未ログイン時: ログインフォームと新規登録フォームが表示され、タブは表示されない
- 「新規登録」フォームから新しいテストユーザー（例: `testuser2`）を作成する → 「登録が完了しました」と表示される
- 作成したテストユーザーでログインする → 7タブが表示される
- ポートフォリオタブで銘柄を1件追加して保存する
- サイドバーの「ログアウト」でログアウトする
- フェーズ1の移行スクリプトで作成した管理者アカウント（`admin`、または移行時に入力したユーザー名）でログインし直す → **`testuser2`で追加した銘柄が見えない**こと（ユーザーごとにデータが分離されていることの確認）、かつ管理者アカウント自身の既存の保有銘柄（フェーズ1で移行した6件）が正しく表示されること
- サイドバーの「パスワード変更」で現在のパスワードを入力し新しいパスワードに変更する → 成功メッセージが表示される。ログアウトして新しいパスワードでログインし直せることを確認する

- [ ] **Step 6: 問題なければバックアップを削除する**

```bash
rm -f data/app.db.backup-before-auth
```

（手動確認で問題が見つかった場合はバックアップから`data/app.db`を復元し、原因を調査する。このステップはコミットを伴わない）
